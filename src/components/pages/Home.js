import React, { Component } from 'react';
import LogoutButton from '../../containers/ui/LogoutButton';

export default class Home extends Component {
	render() {
		return (
			<div>
				<LogoutButton />
			</div>
		);
	}
}